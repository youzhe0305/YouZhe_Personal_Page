# ShellBench project page

Self-contained and ready to copy. Preview with `python -m http.server 8000 --directory project-page`, then open http://localhost:8000. No build step is required. Google Fonts are optional.
